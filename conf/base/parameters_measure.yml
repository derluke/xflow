# This is a boilerplate parameters config generated for pipeline 'measure'
# using Kedro 0.19.3.
#
# Documentation for this file format can be found in "Parameters"
# Link: https://docs.kedro.org/en/0.19.3/configuration/parameters.html

measure_config:
  metric_config:
    binarize_data:
      operator: ">"
      threshold: 113
    weights: weights
  metrics:
    - generalized_f1
    - mean_squared_error
    - datarobot_metrics
